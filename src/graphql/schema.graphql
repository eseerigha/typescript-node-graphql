   type Query {
      info: String!
      feed: [Link!]!
   }

   type User {
     id: ID!
     name: String!
     email: String!
     password: String!
     links: [Link!]!
   }
   
   type Link {
     id: ID!
     description: String!
     url: String!
   }

   type Vote {
      id: ID!
      link: Link!
      user: User!
   }


   type Mutation {
      createLink(url: String!, description: String!): Link!
   }
